[project]
name = "discrete-math-dtu-tools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14.5"
dependencies = [
    "ipykernel>=7.2.0",
    "numpy>=2.4.5",
    "polars>=1.40.1", # Better, Faster, Stronger than pandas
    "sympy>=1.14.0",
]
